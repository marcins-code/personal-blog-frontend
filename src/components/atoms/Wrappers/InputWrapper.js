import styled from 'styled-components';

const InputWrapper = styled.div`
  padding: 10px;
  margin-bottom: 25px;
  position: relative;
  display: flex;
  flex-direction: column;
`;

export default InputWrapper;

// TODO improve positioning with props
